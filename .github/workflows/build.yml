name: build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Running unit test
        run: |
          npm --prefix genealogy ci
          npm --prefix genealogy test

